# =============================================================================
# Mobius Web Scraper - Environment Configuration
# =============================================================================
# Uses mobius-os-dev GCP project. See mobius-config/.env.example for full reference.
# =============================================================================

# -----------------------------------------------------------------------------
# REDIS (local dev = prod-like; cloud for VPN/tunnel)
# -----------------------------------------------------------------------------
# Local (recommended): brew services start redis
REDIS_URL=redis://localhost:6379/0
# Cloud direct (VPN): REDIS_URL=redis://10.40.102.67:6379/0
# Cloud via tunnel: REDIS_URL=redis://127.0.0.1:6380/0 + REDIS_TUNNEL_JUMP_INSTANCE (see mstart)

# -----------------------------------------------------------------------------
# GCS BUCKET
# -----------------------------------------------------------------------------
GCS_BUCKET=mobius-rag-uploads-dev

# -----------------------------------------------------------------------------
# SCRAPER QUEUE KEYS
# -----------------------------------------------------------------------------
SCRAPER_REQUEST_KEY=mobius:scraper:requests
SCRAPER_RESPONSE_KEY_PREFIX=mobius:scraper:response:
SCRAPER_RESPONSE_TTL_SECONDS=86400

# -----------------------------------------------------------------------------
# TREE SCAN LIMITS
# -----------------------------------------------------------------------------
TREE_MAX_DEPTH=3
TREE_MAX_PAGES=50
SCOPE_MODE=same_domain
DOCUMENT_TYPES=pdf,doc,docx,xls,xlsx,ppt,pptx

# -----------------------------------------------------------------------------
# VERTEX AI (for summaries)
# -----------------------------------------------------------------------------
VERTEX_PROJECT_ID=mobius-os-dev
# GOOGLE_APPLICATION_CREDENTIALS=credentials/mobiusos-new-090a058b63d9.json
